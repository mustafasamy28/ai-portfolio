# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# EmailJS Configuration (for contact form)
# Get these from: https://www.emailjs.com/
# IMPORTANT: For Vite, client-side variables must be prefixed with VITE_
VITE_REACT_APP_SERVICE_ID=your-service-id
VITE_REACT_APP_TEMPLATE_ID=your-template-id
VITE_REACT_APP_PUBLIC_KEY=your-public-key

# Note: In production, these should be set in Vercel Environment Variables
# For local development, create a .env file with your actual values
# DO NOT commit .env to git - it's already in .gitignore
